#pragma once
#include <vector>
#include <string>

using std::vector;
using std::string;

class LCPN {
public:
	vector<string> letterCombinations1(string digits);
	vector<string> letterCombinations2(string digits);
};
